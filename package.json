{
  "name": "@unlimiting/payload-cli",
  "version": "0.2.1",
  "description": "Payload CMS CLI for auth and collection operations",
  "type": "module",
  "bin": {
    "payload": "src/index.js"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "lint": "node --check src/index.js && node --check src/lib/config.js && node --check src/lib/payload.js",
    "test": "npm run lint"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "commander": "^13.1.0"
  }
}
