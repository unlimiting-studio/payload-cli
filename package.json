{
  "name": "@unlimiting/payload-cli",
  "version": "0.1.0",
  "description": "CLI for Payload CMS auth, media uploads, and post creation",
  "type": "module",
  "bin": {
    "payload-cli": "./src/index.js"
  },
  "scripts": {
    "lint": "node --check src/index.js && node --check src/lib/config.js && node --check src/lib/payload.js",
    "test": "npm run lint"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "commander": "^13.1.0",
    "form-data": "^4.0.4"
  }
}
